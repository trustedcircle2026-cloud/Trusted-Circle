import { LogOut, Mail, ShieldCheck, UserRound } from 'lucide-react'

export default function ProfilePage({ user, name, setName, onSave, onLogout, onOrders }) {
  return <main className="page-shell"><div className="page-banner compact"><span className="eyebrow">MY PROFILE</span><h1>Your account, in one place.</h1><p>Manage your shopping identity and account details.</p></div><div className="profile-grid"><section className="profile-card"><div className="profile-avatar"><UserRound size={28}/></div><span className="eyebrow">PROFILE DETAILS</span><h2>{user?.name || 'Complete your profile'}</h2><label>Name<input value={name} onChange={e => setName(e.target.value)} placeholder="Your full name"/></label><label>Email<div className="readonly-field"><Mail size={16}/>{user?.email}</div></label><div className="profile-id"><ShieldCheck size={17}/><div><small>TRUSTED CIRCLE USER ID</small><strong>{user?.userId || user?.UserID || '—'}</strong></div></div><button className="btn-primary wide" onClick={onSave}>Save profile</button></section><section className="profile-side"><button className="profile-action" onClick={onOrders}><div><strong>Orders</strong><span>View your order history and status.</span></div><span>→</span></button><button className="profile-action danger" onClick={onLogout}><div><strong>Sign out</strong><span>End this browser session securely.</span></div><LogOut size={18}/></button></section></div></main>
}
