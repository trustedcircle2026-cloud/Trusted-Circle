import { useState } from 'react'
import { CheckCircle2, ChevronDown, Copy, Download, ExternalLink, FileText, PackageCheck, QrCode, Smartphone, X } from 'lucide-react'
import { api } from '../api'
import './orders-page.css'
const money=value=>`₹${Number(value||0).toLocaleString('en-IN',{maximumFractionDigits:2})`
const UPI_URI='upi://pay?pa=paytm.s2eunub@pty&pn=Paytm&tn=Verified Paytm Account'
const invoiceReady=o=>String(o.Status||'').toUpperCase()==='DELIVERED'&&['VERIFIED','PAID'].includes(String(o.PaymentStatus||'').toUpperCase())
function downloadBase64Pdf(base64,fileName){const bytes=Uint8Array.from(atob(base64),c=>c.charCodeAt(0));const blob=new Blob([bytes],{type:'application/pdf'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=fileName||'Trusted-Circle-Invoice.pdf';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
export default function OrdersPage({orders,onBack}){
 const [open,setOpen]=useState(null),[pay,setPay]=useState(null),[copied,setCopied]=useState(false),[invoiceLoading,setInvoiceLoading]=useState(''),[invoiceError,setInvoiceError]=useState('')
 const pending=o=>String(o.PaymentStatus||'PENDING').toUpperCase()==='PENDING'&&!['PAID','DELIVERED','CANCELLED','REFUNDED'].includes(String(o.Status||'').toUpperCase())
 const copy=async text=>{try{await navigator.clipboard.writeText(text);setCopied(true);setTimeout(()=>setCopied(false),1500)}catch{}}
 const downloadInvoice=async order=>{setInvoiceError('');setInvoiceLoading(order.OrderID);try{const token=localStorage.getItem('tc_session');const result=await api.createInvoicePdf(token,order.OrderID);if(!result?.base64)throw new Error('Invoice PDF was not returned.');downloadBase64Pdf(result.base64,result.fileName)}catch(error){setInvoiceError(error.message||'Unable to prepare invoice.')}finally{setInvoiceLoading('')}}
 return <main className="orders-page"><div className="page-shell"><div className="page-banner compact"><span className="eyebrow">MY ORDERS</span><h1>Your voucher orders.</h1><p>Track payment, delivery and your completed-order invoices from one place.</p></div>{invoiceError&&<div className="invoice-error"><FileText size={16}/><span>{invoiceError}</span><button onClick={()=>setInvoiceError('')}><X size={14}/></button></div>}{orders.length?<div className="orders-list">{orders.map(o=>{const expanded=open===o.OrderID;const items=o.Items||[];const ready=invoiceReady(o);return <article className={`order-card order-card-rich ${expanded?'expanded':''}`} key={o.OrderID}><button className="order-main" onClick={()=>setOpen(expanded?null:o.OrderID)}><div className="order-icon"><PackageCheck size={20}/></div><div className="order-copy"><span>{o.OrderNumber}</span><h3>{String(o.Status||'ORDER').replaceAll('_',' ')}</h3><small>{o.CreatedAt?new Date(o.CreatedAt).toLocaleString('en-IN'):''}</small></div><div className="order-right"><strong>{money(o.Total)}</strong><span className={`payment-state ${pending(o)?'pending':ready?'delivered':'paid'}`}>{pending(o)?'Payment Pending':ready?'Completed & Delivered':'Payment '+String(o.PaymentStatus||'PAID')}</span></div><ChevronDown size={17} className="order-chevron"/></button>{expanded&&<div className="order-details"><div className="detail-head"><div><span className="eyebrow">ORDER DETAILS</span><h3>{o.OrderNumber}</h3></div><div className="order-detail-actions">{pending(o)&&<button className="make-payment-btn" onClick={()=>setPay(o)}><Smartphone size={15}/> Make Payment</button>}{ready?<button className="invoice-download-btn" disabled={invoiceLoading===o.OrderID} onClick={()=>downloadInvoice(o)}><Download size={15}/>{invoiceLoading===o.OrderID?'Preparing PDF…':'Download Invoice PDF'}</button>:<span className="invoice-locked"><FileText size={14}/> Invoice unlocks after payment & voucher delivery</span>}</div></div><div className="detail-grid"><div><span>Order ID</span><b>{o.OrderID}</b></div><div><span>Payment Status</span><b>{String(o.PaymentStatus||'PENDING')}</b></div><div><span>Order Status</span><b>{String(o.Status||'')}</b></div><div><span>Subtotal</span><b>{money(o.Subtotal)}</b></div><div><span>Discount</span><b>{money(o.Discount)}</b></div><div><span>Total</span><b>{money(o.Total)}</b></div></div><div className="detail-items"><strong>Items</strong>{items.length?items.map((item,i)=><div className="detail-item" key={item.OrderItemID||i}><span>{item.ProductID}</span><span>Denomination {money(item.Denomination||item.FaceValue)}</span><span>× {item.Quantity}</span><b>{money(item.Total)}</b></div>):<p>Item details will appear here.</p>}</div>{o.PaymentLink?.Link&&<div className="saved-payment-link"><div><span>PAYMENT LINK</span><b>{o.PaymentLink.Label||'Secure payment link'}</b></div><a href={o.PaymentLink.Link} target="_blank" rel="noreferrer">Open Payment Link <ExternalLink size={14}/></a></div>}{o.Payment?.Provider&&<div className="payment-record"><CheckCircle2 size={15}/><span>Latest payment method: <b>{o.Payment.Provider}</b>{o.Payment.ProviderPaymentID&&<> · Reference <b>{o.Payment.ProviderPaymentID}</b></>}</span></div>}{ready&&<div className="invoice-ready-banner"><FileText size={19}/><div><strong>Invoice ready</strong><span>Completed order with verified payment and delivered voucher. Your PDF contains the complete order, payment and voucher record.</span></div><button onClick={()=>downloadInvoice(o)} disabled={invoiceLoading===o.OrderID}>{invoiceLoading===o.OrderID?'Preparing…':'Download PDF'}</button></div>}</div>}</article>})}</div>:<div className="empty-panel"><PackageCheck size={30}/><h3>No orders yet</h3><p>Your checkout orders will appear here.</p><button className="btn-primary" onClick={onBack}>Browse vouchers</button></div>}</div>{pay&&<div className="order-pay-overlay" onClick={()=>setPay(null)}><div className="order-pay-modal" onClick={e=>e.stopPropagation()}><button className="order-pay-close" onClick={()=>setPay(null)}><X size={18}/></button><span className="eyebrow">MAKE PAYMENT</span><h2>{money(pay.Total)} payable</h2><p>Order <b>{pay.OrderNumber}</b> is still pending payment.</p><div className="pay-choice-grid"><a className="pay-choice" href={UPI_URI}><Smartphone size={22}/><b>UPI Apps</b><small>Open your UPI app</small></a><button className="pay-choice" onClick={()=>setPay({...pay,showQr:true})}><QrCode size={22}/><b>QR Code</b><small>Scan with any UPI app</small></button></div>{pay.showQr&&<div className="mini-qr"><img src="https://raw.githubusercontent.com/trustedcircle2026-cloud/Trusted-Circle/main/UPIQR.jpg" alt="UPI QR"/><span>Scan to pay {money(pay.Total)}</span></div>}{pay.PaymentLink?.Link&&<a className="payment-link-big" href={pay.PaymentLink.Link} target="_blank" rel="noreferrer"><ExternalLink size={17}/><span><b>{pay.PaymentLink.Label||'Pay Online'}</b><small>Open payment link</small></span></a>}<div className="upi-id-row"><span>UPI ID: <b>paytm.s2eunub@pty</b></span><button onClick={()=>copy('paytm.s2eunub@pty')}><Copy size={14}/>{copied?'Copied':'Copy'}</button></div><small className="pay-warning">Payment is marked successful only after verified gateway/payment confirmation.</small></div></div>}</main>
}
