import { ArrowLeft, Minus, Plus, ShoppingBag, Trash2 } from 'lucide-react'
import BrandLogo from '../components/BrandLogo'

const money = value => `₹${Number(value || 0).toLocaleString('en-IN', { maximumFractionDigits: 2 })}`

export default function CartPage({ items, brandName, subtotal, savings, total, onQty, onRemove, onContinue, onCheckout }) {
  return <main className="page-shell"><div className="page-banner compact"><button className="back-link" onClick={onContinue}><ArrowLeft size={16}/> Continue shopping</button><span className="eyebrow">YOUR CART</span><h1>Your saved vouchers.</h1><p>{items.length} voucher line{items.length === 1 ? '' : 's'} ready for checkout.</p></div>{items.length ? <div className="cart-layout-page"><div className="cart-lines">{items.map(item => <article className="cart-line-card" key={item.CartID}><BrandLogo name={brandName(item.product.BrandID)} size="lg"/><div className="cart-line-copy"><span>{brandName(item.product.BrandID)}</span><h3>{item.product.Title}</h3><strong>{money(item.product.SellingPrice)}</strong></div><div className="qty-control"><button onClick={() => onQty(item, Number(item.Quantity) - 1)} disabled={Number(item.Quantity) <= 1}><Minus size={15}/></button><b>{item.Quantity}</b><button onClick={() => onQty(item, Number(item.Quantity) + 1)}><Plus size={15}/></button></div><button className="remove-line" onClick={() => onRemove(item)}><Trash2 size={16}/></button></article>)}</div><aside className="summary-card"><span className="eyebrow">ORDER SUMMARY</span><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><div className="saving-row"><span>Savings</span><strong>- {money(savings)}</strong></div><hr/><div className="summary-total"><span>Total</span><strong>{money(total)}</strong></div><button className="btn-primary wide" onClick={onCheckout}>Proceed to payment <ShoppingBag size={16}/></button></aside></div> : <div className="empty-panel"><ShoppingBag size={30}/><h3>Your cart is empty</h3><p>Add a voucher and it will stay linked to your account.</p><button className="btn-primary" onClick={onContinue}>Browse vouchers</button></div>}</main>
}
