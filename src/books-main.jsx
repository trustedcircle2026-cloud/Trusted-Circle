import React,{useEffect,useState} from 'react'
import ReactDOM from 'react-dom/client'
import {ArrowRight,BookOpen,CheckCircle2,ChevronRight,LockKeyhole,Mail,ShieldCheck} from 'lucide-react'
import {BOOKS_API_URL,BOOKS_SESSION_KEY} from './books-config'
import BooksWorkspaceStable from './BooksWorkspaceStable'
import BooksOnboarding from './BooksOnboarding'
import './books.css'
import './books-workspace-stable.css'
const LOGO='https://raw.githubusercontent.com/trustedcircle2026-cloud/Trusted-Circle/main/Logo%20new.jpg'
async function request(action,payload={}){if(!BOOKS_API_URL)throw Error('Trusted Circle Books backend URL is not configured.');const r=await fetch(BOOKS_API_URL,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action,...payload})});const text=await r.text();let data;try{data=JSON.parse(text)}catch(e){throw Error('Books backend returned an invalid response.')};if(!r.ok||data.ok===false)throw Error(data.error?.message||data.message||'Books request failed.');return data.data??data}
function Login({onLogin}){const[email,setEmail]=useState(''),[otp,setOtp]=useState(''),[step,setStep]=useState('email'),[busy,setBusy]=useState(false),[msg,setMsg]=useState(''),[error,setError]=useState(false);const send=async e=>{e.preventDefault();setBusy(true);setError(false);setMsg('');try{await request('booksRequestOtp',{email:email.trim().toLowerCase()});setStep('otp');setMsg('Verification code sent to '+email.trim()+'.')}catch(e){setError(true);setMsg(e.message)}finally{setBusy(false)}};const verify=async e=>{e.preventDefault();setBusy(true);setError(false);setMsg('');try{const x=await request('booksVerifyOtp',{email:email.trim().toLowerCase(),otp});if(!x.session?.token)throw Error('Books session was not returned by the backend.');localStorage.setItem(BOOKS_SESSION_KEY,x.session.token);onLogin(x.user,x)}catch(e){setError(true);setMsg(e.message)}finally{setBusy(false)}};return <main className="books-login-shell"><section className="books-login-card"><div className="books-brand"><span className="books-logo"><img src={LOGO} alt="Trusted Circle"/></span><div><strong>Trusted Circle Books</strong><small>Cloud Accounting</small></div></div><div className="books-eyebrow"><ShieldCheck size={14}/> SECURE BOOKS ACCESS</div><h1>Run your business<br/><span>with complete control.</span></h1><p className="books-lead">Accounting, invoicing, purchases, banking, GST, inventory and financial reporting — one Trusted Circle workspace.</p><div className="books-login-form">{step==='email'?<form onSubmit={send}><label>Work email</label><div className="books-input"><Mail size={17}/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></div><button className="books-primary" disabled={busy}>{busy?'Sending…':<>Continue with email <ArrowRight size={17}/></>}</button></form>:<form onSubmit={verify}><label>Verification code</label><div className="books-input"><LockKeyhole size={17}/><input inputMode="numeric" pattern="[0-9]{6}" maxLength="6" required value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,''))} placeholder="6-digit OTP"/></div><button className="books-primary" disabled={busy}>{busy?'Verifying…':<>Verify & continue <ArrowRight size={17}/></>}</button><button type="button" className="books-secondary" onClick={()=>{setStep('email');setMsg('')}}>Use another email</button></form>}{msg&&<div className={'books-message '+(error?'error':'success')}>{msg}</div>}</div><div className="books-trust"><CheckCircle2 size={16}/><span>Organisation access · Role-based permissions · Audit-ready records</span></div></section><aside className="books-side"><span className="books-pill"><BookOpen size={14}/> TRUSTED CIRCLE BOOKS</span><h2>One system for your<br/><em>entire financial year.</em></h2><p>FY-wise accounting, GST-ready workflows and management reporting.</p><div className="books-feature-list"><div><BookOpen size={18}/><span><b>Complete Accounting</b><small>Ledgers, journals, sales, purchases and reports</small></span><ChevronRight size={15}/></div><div><ShieldCheck size={18}/><span><b>Organisation Control</b><small>Users, employees, roles and approvals</small></span><ChevronRight size={15}/></div><div><CheckCircle2 size={18}/><span><b>GST & Banking</b><small>Tax, reconciliation and TDS workflows</small></span><ChevronRight size={15}/></div></div><footer>© 2026 Trusted Circle · Books</footer></aside></main>}
function active(user,x){const a=x?.organisationAccess||user?.organisationAccess;return a==='ACTIVE'||user?.organisationStatus==='ACTIVE'||user?.membershipStatus==='ACTIVE'||(Array.isArray(x?.memberships)&&x.memberships.some(m=>String(m.Status||'').toUpperCase()==='ACTIVE'))}
function pending(user,x){const a=x?.organisationAccess||user?.organisationAccess;return a==='PENDING'||user?.organisationStatus==='PENDING'||user?.membershipStatus==='PENDING'||(Array.isArray(x?.memberships)&&x.memberships.some(m=>String(m.Status||'').toUpperCase()==='PENDING'))}
function App(){const[user,setUser]=useState(null),[payload,setPayload]=useState(null),[checking,setChecking]=useState(true);useEffect(()=>{const t=localStorage.getItem(BOOKS_SESSION_KEY);if(!t){setChecking(false);return}request('booksMe',{token:t}).then(x=>{setUser(x.user);setPayload(x)}).catch(()=>localStorage.removeItem(BOOKS_SESSION_KEY)).finally(()=>setChecking(false))},[]);const logout=async()=>{const t=localStorage.getItem(BOOKS_SESSION_KEY);try{if(t)await request('booksLogout',{token:t})}catch(e){}localStorage.removeItem(BOOKS_SESSION_KEY);setUser(null);setPayload(null)};if(checking)return <div className="bw-loading">Opening Trusted Circle Books…</div>;if(!user)return <Login onLogin={(u,x)=>{setUser(u);setPayload(x)}}/>;if(active(user,payload))return <BooksWorkspaceStable user={user} onLogout={logout}/>;return <BooksOnboarding user={user} accessState={pending(user,payload)?'PENDING':'NEW'} onComplete={x=>{if(x?.user)setUser(x.user);if(x)setPayload(x);if(x?.session?.token)localStorage.setItem(BOOKS_SESSION_KEY,x.session.token)}} onLogout={logout}/>}
ReactDOM.createRoot(document.getElementById('books-root')).render(<App/>)
